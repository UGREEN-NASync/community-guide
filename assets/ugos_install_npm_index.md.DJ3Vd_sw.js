import{_ as s,c as a,a2 as o,o as t}from"./chunks/framework.C57abF2z.js";const u=JSON.parse('{"title":"Setting up Nginx Proxy Manager","description":"","frontmatter":{},"headers":[],"relativePath":"ugos/install/npm/index.md","filePath":"ugos/install/npm/index.md"}'),i={name:"ugos/install/npm/index.md"};function n(c,e,d,l,r,p){return t(),a("div",null,e[0]||(e[0]=[o(`<h1 id="setting-up-nginx-proxy-manager" tabindex="-1">Setting up Nginx Proxy Manager <a class="header-anchor" href="#setting-up-nginx-proxy-manager" aria-label="Permalink to &quot;Setting up Nginx Proxy Manager&quot;">​</a></h1><div class="info custom-block"><p class="custom-block-title">NOTE</p><p>This guide comes with an optional dashboard for NPM using goaccess. To function correctly it accesses the NPM logs as specified in the <code>compose.yaml</code>.</p></div><p>You can omit the log dashbaord by deleting or commenting the goaccess section in the <code>compose.yaml</code></p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">service</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    npm-app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        ...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        ...</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    # goaccess:</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    #     ...</span></span></code></pre></div><h2 id="instructions" tabindex="-1">Instructions <a class="header-anchor" href="#instructions" aria-label="Permalink to &quot;Instructions&quot;">​</a></h2><p>Create a new directory called <code>npm</code> within the docker folder and create a new <code>compose.yaml</code> file. The docker compose file for Nginx Proxy Manager (NPM) can be found <a href="https://raw.githubusercontent.com/UGREEN-NASync/community-guide/refs/heads/main/docs/ugos/install/npm/compose.yaml" target="_blank" rel="noreferrer">here</a>.</p><div class="info custom-block"><p class="custom-block-title">NOTE</p><p>In your docker app on UGOS create a new network called <code>proxy</code>. This network can be used with other docker services to enable container name resolution (&quot;docker DNS&quot;).</p></div><p>Save the yaml file and create a new file called <code>.env</code> in the npm directory. Include the following two lines to set the database passwords</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">NPM_DB_PW=</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # use &#39; &#39; around the password if it contains special characters</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">NPM_DB_ROOT_PW=</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  # use &#39; &#39; around the password if it contains special characters</span></span></code></pre></div><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>You can use the command <code>openssl rand -base64 48</code> to generate a random string for passwords, keys or secrets if required.</p></div><p>After saving both files, right click on the npm folder in the VSCode Explorer and click <code>Open in integrated terminal</code>. This will open a terminal windows which is already located in the correct directory.</p><div class="tip custom-block"><p class="custom-block-title">TIP</p><p>To paste into the terminal use <code>SHIFT+CTRL+V</code> OR <code>right mouse button</code>.</p></div><p>You can now launch the container by executing</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> docker</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> compose</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> up</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -d</span></span></code></pre></div><p>Next, setup NPM as shown in your browser. Use the default credentials to setup your admin account in NPM. Connect to it using <code>http://LOCAL_SERVER_IP:8181</code> OR any other external port that you specified for the default port 81 in the <code>compose.yaml</code> file.</p><div class="info custom-block"><p class="custom-block-title">NOTE</p><p>The default login credentials for NPM are <code>admin@example.com</code> and <code>changeme</code></p></div><h2 id="adding-proxy-hosts" tabindex="-1">Adding proxy hosts <a class="header-anchor" href="#adding-proxy-hosts" aria-label="Permalink to &quot;Adding proxy hosts&quot;">​</a></h2><ol><li>Navigate to <code>SSL Certificates</code>, click <code>Add SSL Certificate</code> and chose <code>Lets Encrypt</code></li><li>Domain name = <code>*.domain.duckdns.org</code> -&gt; <code>Use DNS Challenge = True</code> -&gt; Agree to the ToS</li><li>It is important to specify <code>*.YOUR_DOMAIN.com</code> as this creates a wildcard certificate for any subsequent subdomain that gets created under the main domain name.</li><li>Chose DuckDNS (or appropriate) as your provider and paste in your Token to replace <code>your-duckdns-token</code> in the <code>Credentials File Content</code></li><li>Save the SSL certificate</li><li>Navigate to &quot;Hosts -&gt; Proxy Hosts&quot; and &quot;Add Proxy Host&quot;</li><li>Specify a domain name such as <code>nextcloud.domain.duckdns.org</code></li><li>Use <code>http</code> as a scheme (for most servcies) and enter your servers local IP adress OR the <code>container_name</code> in &quot;Forward Hostname / IP&quot; and the external port of the service (specified in the docker-compose.yaml) in &quot;Forward Port&quot;</li><li>Activate the toggles for &quot;Block Common Exploits&quot;</li><li>Navigate to the &quot;SSL&quot; tab and choose the previousyl created wildcard certificate</li><li>Enable <code>Force SSL</code>, <code>HTTP/2 Support</code>, <code>HSTS enabled</code>, <code>HSTS subdomains</code></li><li>Click save to save the new proxy host</li></ol><div class="warning custom-block"><p class="custom-block-title">IMPORTANT</p><p>Make sure that the external https port that you specified for NPM in the docker-compose.yaml (e.g. <code>4443:443</code> OR <code>443:443</code> OR <code>ANY_PORT:443</code>) is forwarded by your router to your server. This should be the only open port on your router! DO NOT open the ports of the individual services directly on your router. This is the whole point of using a reverse proxy such as NPM!</p></div><div class="info custom-block"><p class="custom-block-title">CREDIT</p><p>This guide was created by <a href="https://github.com/vzvl" target="_blank" rel="noreferrer">vzvl</a></p></div>`,20)]))}const k=s(i,[["render",n]]);export{u as __pageData,k as default};
